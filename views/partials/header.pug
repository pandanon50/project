header.header
    .header__column
        a(href=routes.home)
            i.fas.fa-wallet
        h1 가계부
    .header__column
        ul
        if !user.isAuthenticated
            li
                a(href=routes.join) Join
            li
                a(href=routes.login) Login
            li
                a(href=`/meet${routes.upload}`) Upload
        else
            li
                a(href=`/meet${routes.upload}`) Upload
            li 
                a(href=routes.logout) Logout